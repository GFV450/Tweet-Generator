def markov(tokenized_corpus):
    markov = {}

    for index, word in enumerate(tokenized_corpus):
        if word in markov:
            print("assa")
        else:
            markov[word] = []
            mark


if __name__ == '__main__':
